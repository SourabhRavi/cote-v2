[
  {
    "id": "dropColumn.ChannelMember.lastReatAt",
    "label": "Drop column \"lastReatAt\" from \"ChannelMember\"",
    "operationClass": "destructive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "lastReatAt",
        "table": "ChannelMember"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"lastReatAt\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "ChannelMember",
          "lastReatAt"
        ]
      }
    ],
    "execute": [
      {
        "description": "drop column \"lastReatAt\"",
        "sql": "ALTER TABLE \"public\".\"ChannelMember\" DROP COLUMN \"lastReatAt\""
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"lastReatAt\" does not exist",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "ChannelMember",
          "lastReatAt"
        ]
      }
    ]
  },
  {
    "id": "column.public.ChannelMember.lastReadAt",
    "label": "Add column \"lastReadAt\" to \"ChannelMember\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "lastReadAt",
        "table": "ChannelMember"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"lastReadAt\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "ChannelMember",
          "lastReadAt"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"lastReadAt\"",
        "sql": "ALTER TABLE \"public\".\"ChannelMember\" ADD COLUMN \"lastReadAt\" timestamptz"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"lastReadAt\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "ChannelMember",
          "lastReadAt"
        ]
      }
    ]
  }
]